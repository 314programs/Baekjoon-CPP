//Need to plant
#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL), cout.tie(NULL);
    
    int over, under;
    cin >> over >> under;

    cout << under-over << " " << under;

}

