//Arrived in Korea
#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL), cout.tie(NULL);

    int ur, tr, uo, to;
    cin >> ur >> tr >> uo >> to;

    long long ans = (56*ur) + (24*tr) + (14*uo) + (6*to);
    cout << ans;

}
