//Multisets store elements in a specific order, and multiple elements can have equivilant values.
//Multiset is always stored in a sorted order by a comparing function, default is smallest to largest

//int (data type)
multiset<int> my_multiset;

//Once inside multiset, value cannot be changed (const variable) but it can be removed or added.

//1 (data type)
my_multiset.insert(1);
//Memory address (my_multiset.begin())
my_multiset.erase(my_multiset.begin());


